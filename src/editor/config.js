// Default canvas resolution — meant to match the target game's resolution.
// Overridable at runtime via PhaserCanvas props (task 3 will wire this to a UI control).
export const DEFAULT_RESOLUTION = {
  width: 1280,
  height: 720,
}
